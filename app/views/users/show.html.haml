= @user.name
= link_to "Downgrade your account", user_path(@user, user: { role: 'standard'} ), method: :patch, class: 'btn btn-danger', data: { confirm: 'Are you sure you want to downgrade?'}
- @wikis_private.each do |wiki|
  .media
    .media-body
      .media-heading
        %p= link_to wiki.title, wiki
- @wikis_public.each do |wiki|
  .media
    .media-body
      .media-heading
        %p= link_to wiki.title, wiki

